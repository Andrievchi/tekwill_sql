--select distinct 
--        salary, 
  --      DEPARTMENT_ID,
   --     EMAIL
--from EMPLOYEES

/*select first_name,
       last_name, 
       salary, 
       salary*1.1, 
       salary + COMMISSION_PCT
from EMPLOYEES */

/*select 10+15-5*2/7
from dual */

/*select first_name,
       last_name, 
       salary, 
       salary*1.1,
       COMMISSION_PCT,
       salary + salary*COMMISSION_PCT
from EMPLOYEES */

describe employees

